# Soil
SOIL is a privacy-first mobile plant disease diagnosis framework that runs CNN models directly in the browser using TF.js. Models are dynamically loaded based on crop type and optimized for lightweight, multi-channel processing, enabling on-device inference without app install keeping user images private and ensuring cross-platform compatibility.
